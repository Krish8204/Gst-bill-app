name: gst_billing_app
description: A GST Billing application.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  sqflite: ^2.3.0
  sqflite_common_ffi_web: ^0.4.0
  path: ^1.8.3
  intl: ^0.18.1
  pdf: ^3.10.4
  path_provider: ^2.1.1
  share_plus: ^7.1.0
  url_launcher: ^6.1.14

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true 